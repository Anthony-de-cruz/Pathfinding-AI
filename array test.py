grid = []

rows = 13
columns = 26

for row in range(rows):

    grid.append([])

    for column in range(columns):

        grid[row].append([0])

for row in range(rows):
    for column in range(columns):
        print("nice")
